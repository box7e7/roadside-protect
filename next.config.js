/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
        "localhost",
        "momentum-roadside.com",
	"jenkins.mehdi.cloud"
    ],
},
}

module.exports = nextConfig


